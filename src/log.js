import snooplogg from 'snooplogg';

const { chalk } = snooplogg;

const log = snooplogg;
    // .stdio

    // .style('error', (s) => chalk.red(s))
    // .style('heading', (s) => String(s).toUpperCase())
    // .style('subheading', (s) => chalk.gray(s));

//const { highlight, note } = log.styles;

export { log };
